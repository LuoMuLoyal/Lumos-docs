# Lucent

> ⚠️ `Lumos-docs` 是展示性文档站，不是权威参考。以下内容来自 `Lucent/docs/` 的副本，可能更新不及时。请以 `Lucent/docs/` 为准。

这里收录 `Lucent/docs` 的内容副本。

左侧侧边栏会根据 `current/lucent-docs` 目录中的 Markdown 文件自动生成。

Lucent 的 OpenAPI 合同会额外生成一组可浏览的 Markdown 页面，入口在 [API](/api/)。

`openapi.json` 仍然只是生成产物快照，不作为这里的手工维护页面。

## 延伸阅读

- [架构文档 (Compodoc)](/compodoc/) — 从 NestJS 源码自动生成的模块依赖图、路由表和类索引
- [API 接口文档](/api/) — OpenAPI 自动生成的接口和 Schema 浏览页面
- [Wiki - 架构概览](/wiki/architecture) — 手写架构设计说明
