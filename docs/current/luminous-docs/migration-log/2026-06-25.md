# Migration Log - 2026-06-25

## Documentation boundary cleanup

Clarified the documentation boundaries across `Lucent/docs/`, `Luminous/docs/`, and `Lumos-docs/`.

### What changed

- Updated `Luminous/docs/README.md` and `Lucent/docs/README.md`:
  - Added explicit `Lumos-docs` disclaimer: it is a slow-updating showcase site, not the authoritative reference.
  - Added the rule that completed goals are deleted from `Next_Plan.md` (not marked complete there) and their resulting state is moved to `Current_State.md`.
- Updated `Lumos-docs/docs/current/index.md`, `lucent.md`, and `luminous.md`:
  - Rewrote the index to state that `Lumos-docs` is not authoritative and that repo docs are the source of truth.
  - Added prominent out-of-date warnings on the Lucent and Luminous landing pages.
- Updated `Luminous/docs/Project_Guardrails.md`:
  - Added the same `Next_Plan.md` deletion rule and the `Lumos-docs` non-reference rule.
- Rewrote `Luminous/docs/Next_Plan.md`:
  - Removed the completed "UI/UX pass and freezed migration" goal statement.
  - Removed the completed "Record fast-entry UX" from the recommended sequence.
  - Kept the document focused on the next active slice: bounded assistant leaflet RAG in Lucent.
- Updated `Luminous/docs/Current_State.md`:
  - Added a "Completed Baselines" section noting UI/UX pass/freezed migration and Record fast-entry UX.

### Why

`Next_Plan.md` was accumulating completed goals and state statements, and `Lumos-docs` copies were being treated as references. This cleanup restores the rule that `Next_Plan.md` only owns the next implementation order, `Current_State.md` owns current truth, and repo docs take precedence over the showcase site.

## TODO and plans cleanup

Audited active TODO files and plans for completed items, moved their state to the appropriate current-state documents, and deleted finished plan files.

### What changed

- Deleted `Luminous/plans/2026-06-13-sleep-contract-slice.md`.
  - The sleep contract slice is fully delivered: `Record` supports real sleep CRUD with structured fields, `Today` reads real sleep duration from persisted data, and `Report` generates real sleep trends instead of hard-coded zero arrays. All milestones are already reflected in `Current_State.md` and prior migration logs (2026-06-13 through 2026-06-22).
- Updated `Lucent/docs/TODO.md`:
  - Removed the completed "JWT/admin secrets now come from env files only" item; this is already true and documented in `README.md` and `environment.md`.
  - Removed the completed "`testing-support` now reuses the shared `ARGON2_OPTIONS`" item; verified in code (`testing-support.service.ts` and its spec now import from `auth/config/argon2-options`).
  - Updated the `user-health-context.service.ts` split note: profile write is now confirmed separated into `UserHealthContextProfileWriteService`, while allergy/condition/current-medicine write normalization still remains in the main service.
- Deleted `Lucent/plans/2026-06-23-prisma-typing-migration.md`.
  - Wave 1 of the Prisma typing migration is complete. The broad `as` cast anti-pattern has been eliminated across assistant, user-health-context, medicine-reminders, and reports modules. Remaining `as` narrowings are all in the allowed categories: JSON payload parsing, test doubles, and object-property narrowing.
- Type-only tightening in `Lucent` (no behavior change):
  - `src/modules/reports/dashboard/reports-context.service.ts` — imported `DailyRecordKind`; tightened `buildWaterSeries`/`buildSleepSeries` parameter types from `kind: string` to `DailyRecordKind`; switched comparisons to `DailyRecordKind.water` / `DailyRecordKind.sleep`.
  - `src/modules/assistant/tools/assistant-tool.constants.ts` — imported `DailyRecordKind`; changed `ToolRecordItem.kind` from `string` to `DailyRecordKind`.
  - `src/modules/assistant/tools/assistant-tool-presenters.ts` — tightened `kind: string` to `DailyRecordKind` in `buildCreateRecordPreviewFields`, `describeCreateRecordSummary`, `describeUpdateRecordSummary`, `describeDeleteRecordSummary`, and `describeRecordTargetLabel`.
