# 2026-06-20

## Record

- Reworked all active Record quick actions (`water / meal / symptom / note / sleep`) so signed-in taps open a quick-choice bottom sheet first instead of jumping straight to the full create page.
- Replaced the old lightweight fast-entry mini-form with direct quick choices plus a `more` action.
- Wired direct quick-choice save to the selected record date plus the real current `HH:mm`.
- Added editable Record date/time controls to both full create and edit pages.
- Updated Record detail and timeline rendering to show concrete saved hour/minute.
- Regenerated the Lucent OpenAPI client after the daily-record contract added persisted `occurredTime`.
- Added new Record ARB strings for quick-choice labels and date/time field labels.
- Updated focused Record tests so quick-action save assertions now verify:
  - fast-entry opens first
  - quick-choice taps save immediately
  - `occurredAt` and `occurredTime` are both sent
  - `more` opens the full create page with seeded date/time
- Updated daily-record remote data source tests to cover `occurredTime` request payloads.

## Planning

- Closed the temporary Record fast-entry plan files after folding stable facts
  into current docs.
