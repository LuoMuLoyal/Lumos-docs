# 2026-06-15

- Wired the Report `给校医院 / For clinic` export card to the real Lucent data-export request flow for `hospital + pdf + last_7_days`, including latest-download-url refresh before opening the file.
- Kept `月度报告 / Monthly` and `打印预览 / Print` visible in Report, but changed them to inactive entries until Lucent exposes distinct real export contracts for those actions.
- Updated Settings export to use the same request body and status handling as the new Report export flow instead of the old empty-body POST assumption.
- Extended `tool/regenerate_lucent_openapi.dart` so client regeneration now patches the OpenAPI generator's broken enum defaults for `CreateDataExportRequestDto` in both source `*.dart` and generated `*.g.dart`.
- Added focused Flutter coverage for export request parameters and Report export link opening.
- Unified Medicine risk-check coverage messaging so the main risk-check page and add-before-save precheck both surface an explicit summary when manual entries or missing source detail cannot be checked.
- Expanded the bounded Medicine rule set for current MVP safety checks: allergy matching now covers a small reviewed Chinese/English alias map, pediatric/geriatric warnings are age-gated, pregnancy/lactation can emit explicit low-confidence warnings when source text is missing, and duplicate-ingredient detection now also covers reviewed DrugBank synonym overlap.
- Added broader Medicine risk-check tests for duplicate ingredients, allergy matching, age-gated special-group warnings, missing-source warnings, and DrugBank synonym overlap.
- Connected the Report `月度报告 / Monthly` and `打印预览 / Print` cards to real Lucent export requests instead of leaving them inactive, with request shapes `monthly + pdf + last_30_days` and `print + pdf + last_7_days`.
- Fixed export in-flight UI state so the active Report export card shows progress for the export kind currently being generated, not only the hospital card.
- Re-scoped MVP docs so campus/help resource completeness, offline-care escalation copy, and future agent/map-assisted support discovery are no longer treated as MVP blockers.
- Aligned the Medicine workspace safety cards with the risk-check page and add-before-save precheck so coverage-gap boundary alerts are no longer hidden whenever findings also exist.
- Added `docs/MVP_Demo_Script.md` as a short operator-facing script for the current real MVP demo path.
- Hardened the full-stack Today/Report AI lane for test and fallback scenarios: Lucent full-stack prepare now also resets `aiSummariesEnabled`, Today/Report streaming AI summaries now fall back cleanly when the analysis model is not configured, and the Flutter AI cards seed a loading summary so E2E no longer depends on the model emitting a partial summary before the final result.
- Added a script-style Flutter task entry via Melos, plus a `.env.fullstack-e2e.example` template and auto-detection in `tool/run_fullstack_checks.ps1`, so the Android full-stack lanes can be run with short commands instead of repeating long `--dart-define` lists.
- Closed the last medicine-safety MVP blocker by making pediatric/geriatric missing-detail cases explicit `unknown` findings too, and by keeping coverage summaries visible even when zero medicine details could be checked.
- Froze the mobile MVP docs around the current reviewed medicine boundary instead of continuing to list medicine-safety hardening as an open MVP blocker.
