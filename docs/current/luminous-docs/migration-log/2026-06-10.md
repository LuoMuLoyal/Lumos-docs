# Migration Log - 2026-06-10

## Medicine Reminder Date Window And Delivery History

- Extended Lucent medicine reminder schedules with optional `startDate` and `endDate` fields.
- Added Lucent `UserReminderDelivery` storage plus read-only `GET /api/v1/me/reminder-deliveries?date=&limit=` for future audit rows; no worker writes rows yet.
- Regenerated the Luminous Lucent OpenAPI package from the updated backend contract; generated baseline is now 39 paths / 109 schemas.
- Wired Luminous reminder reads/writes through the new date-window fields while preserving raw Dio writes for nullable reminder behavior.
- Added Medicine reminder create/edit rows for start date, end date, end-date clearing, and date-range validation.
- Added a local reminder sound preference stored on-device with `SharedPreferences`.
- Added a flat reminder method panel showing local notification state, SMS unavailable state, and selected local sound.
- Added reminder detail delivery-history display backed by Lucent's read-only delivery-log API, including a true empty state when no audit rows exist.
- Updated Chinese and English ARB strings and regenerated Flutter localizations.
- Updated reminder data-source and page tests for date windows, local sound, SMS unavailable state, and delivery-history UI.

## Medicine Reminder Local Notifications

- Added a three-layer local notification path for medicine reminders: pure planner, platform gateway, and a Riverpod sync coordinator.
- Added a watch-style sync provider kept alive from `LuminousApp`, so auth restore, reminder list invalidation, notification permission changes, and local sound changes all reschedule automatically without page-level calls.
- Scheduled concrete reminder instances for the next 7 days and capped the pending queue at 60 entries to stay below iOS pending-notification limits.
- Added deterministic per-occurrence notification IDs plus persisted scheduled-ID bookkeeping so resync cancels only medicine reminder notifications instead of calling `cancelAll()`.
- Added dual Android channels for sounding and silent reminder delivery; `gentle` currently maps to the same system-default sound path as `default`.
- Added four localization keys for the fallback local notification title/body and the device channel name/description.
- Added planner and coordinator test coverage, plus app/provider wiring so reminder schedules now trigger real on-device local notifications while push, SMS, and backend delivery workers remain out of scope.

## Record Form Maintenance

- Added `RecordEntryType.note` and `RecordCopyKey.typeNote` to give note records their own client-side type instead of being aliased to medication.
- Fixed `recordEntryTypeForDailyRecordKind(DailyRecordKind.note)` to map to `RecordEntryType.note` instead of `RecordEntryType.medication`.
- Added `dailyRecordKindForEntryType(RecordEntryType.note)` mapping to `DailyRecordKind.note` so the note quick action routes to create.
- Activated `RecordEntryType.note` in both `LucentRecordRepository` and `MockRecordRepository` so note records appear in timelines, filters, and quick actions.
- Added note quick action and filter chip to both repositories.
- Removed the misleading `_staticTimeline` fallback from `LucentRecordRepository.fetchDashboard`; empty real results now produce an empty timeline instead of swapping back to mock entries.
- Removed the artificial `.take(5)` cap from `_mobileQuickActions` so all active quick actions render across multiple rows.
- Added `note` to mobile quick-action ordering, filter ordering, and overview type order.
- Added regression tests: sleep toast-only behavior, note quick action create flow, note mapping unit tests, and Lucent empty-filter-results test.
- All 162 tests pass; `flutter analyze` clean.

## Note Timeline Title Localization Fix

- Fixed `LucentRecordRepository._toTimelineEntry` to use type-dependent `titleKey` instead of hardcoded `RecordCopyKey.typeMood` for all timeline entries.
- Note records without a real title now leave `rawTitle` null so the timeline resolves through the localized `RecordCopyKey.typeNote` instead of showing the raw enum name `note`.
- Removed hardcoded English `rawTitle: 'Note'` from the mock note timeline entry in `MockRecordRepository`.
- Added regression tests: note without title falls back to localized key, note with title uses it as rawTitle, water entry uses typeWater titleKey.
- All 165 tests pass; `flutter analyze` clean.

## Repo-Local Plan Workflow

- Stopped using workspace-root `PLANS.md` for active implementation plans.
- Added repo-local `plans/README.md` entry points in both `Luminous` and `Lucent`.
- Documented the rule that active multi-step execution plans belong in the owning repo's `plans/` directory, while stable facts still belong in `docs/`.
- Kept `docs/Next_Plan.md` as the repo-level ordering document only; task-specific execution detail should move into `plans/*.md` during active work and be deleted after the owning docs are updated.

## Mine And Settings Contracts Planning

- Marked Report data contracts as paused by user direction in `docs/Next_Plan.md`.
- Promoted Mine and Settings contracts to the active next work item.
- Added `plans/2026-06-10-mine-settings-contracts.md` with the first-pass contract scope, implementation phases, verification commands, and review blockers.
- Scoped the work around remaining static or toast-only Mine/Settings areas: campus services, privacy permissions, reminder preference sync, data export request/status, help content, and about metadata.

## Mine And Settings Contracts — Lucent Backend (Phase 1 & 2)

- Created `Lucent/docs/public/mine-settings-contract.md` defining endpoints, ownership map, Prisma models, and non-goals.
- Added Prisma `UserSetting` model (one row per user per key, JSONB value) and `DataExportRequest` model (status tracking without file generation).
- Added `user-settings` module: `GET /api/v1/me/settings` returns `aiSummariesEnabled` and `dataSharingConsent`; `PATCH /api/v1/me/settings` does partial upsert.
- Added `support-resources` module: `GET /api/v1/public/support-resources?scope=` serves static campus/help entries; `GET /api/v1/public/app-info` returns package metadata.
- Added `data-export` module: `POST /api/v1/me/data-export-requests` creates a `requested` status row; `GET /api/v1/me/data-export-requests/latest` returns the newest request.
- All three modules include controller tests; 193 tests pass.
- `pnpm lint:check` clean; `pnpm build` clean; OpenAPI export: 44 paths / 118 schemas.
