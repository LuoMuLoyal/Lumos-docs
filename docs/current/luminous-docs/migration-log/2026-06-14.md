# Migration Log - 2026-06-14

- Added phase-1 natural-language record intake on mobile Record: the existing AI input bar and a new FAB now open one shared bottom sheet.
- Wired the bottom sheet to Lucent `POST /api/v1/user/daily-records/candidate-records/generate`, shows removable candidate records, and saves confirmed items through the existing daily-record create flow.
- Extended the local daily-record repository/data source boundary with candidate-record entities and generated-client usage for AI parsing.
- Regenerated `packages/lucent_openapi` against the latest Lucent spec and patched the generated package export/getter gap for `AppApi` so the app network boundary stays consistent.
- Added Record ARB strings for the natural-language sheet actions, counts, and save toasts, then regenerated Flutter l10n output.
- Upgraded Record natural-language candidate review from delete-only to editable/selective save: candidate title/value/unit/note can be changed, sleep duration/quality can be corrected, and unselected items are skipped.
- Changed save flow from save-all to save-selected, and partial save failures now keep only failed candidate drafts in place with item-level failure hints so the user can retry instead of losing the batch.
- Added focused controller and widget coverage for the Record natural-language review/save flow.
- Polished candidate editors by type for MVP use: water now uses numeric amount + shared unit dropdown, meal/symptom labels are more specific, and note candidates use a clearer content-focused field.
- Added widget coverage for switching a water candidate unit in the NLP sheet and persisting the changed unit through save.
- Added `docs/TODO.md` to separate remaining MVP gaps from current-state docs, then aligned `Current_State.md`, `Next_Plan.md`, `docs/README.md`, and `Product_Vision.md` around the current MVP boundary.
- Corrected the current OpenAPI contract baseline in docs and narrowed the remaining still-fake MVP-facing surfaces to Report export actions and Mine campus-service taps.
- Added real Today/Report AI streaming: Lucent now exposes SSE endpoints for both summary flows, Flutter consumes them with a shared manual SSE parser, and the cards render growing summary text before the final structured result lands.
- Regenerated `packages/lucent_openapi` against the new stream DTO contract and patched the regeneration wrapper so missing generated `AppApi` exports/getters are repaired automatically instead of breaking the app after future codegen runs.
- Extended the Android emulator full-stack Today/Report lane so it now triggers both real AI generate actions and asserts the streaming-state transition before final success.
- Fixed a mobile-width overflow in the Report AI summary header by letting the range switcher and action button wrap under the title block instead of forcing a single-row layout.
- Added a real Medicine risk-check MVP flow in Flutter: current-medicine risk results now come from existing health-context data plus Lucent medicine-detail lookups, the `/medicine/risk-check` page renders findings and coverage gaps, and the quick action no longer resolves to a toast.
- Wired Medicine workspace safety cards to the same structured risk-check result instead of fixed placeholder alerts, while keeping result-to-copy mapping in the presentation layer so repository code stays language-agnostic.
- Added medicine risk-check unit/widget coverage and kept the repo passing with `flutter analyze` plus `flutter test test/medicine`.
- Added add-before-save medicine risk precheck on search results: the app now evaluates the pending candidate against the current health-context snapshot, shows a confirmation sheet when findings or coverage gaps exist, and lets a clear result save directly.
- Added focused search-page coverage for both precheck branches and made the warning sheet scroll safely on smaller/mobile-height layouts.
- Reworked Mine campus/support resources to use explicit `actionType` + `actionTarget` fields, keep rows visible when Lucent has no target, and disable taps instead of showing fake placeholder actions.
- Added Mine widget coverage for the no-target campus support state.
- Added an explicit failed-item retry path inside the Record natural-language sheet: partial save failures now keep their candidate drafts visible with item-level failure hints plus a “retry failed items” action instead of relying on toast memory alone.
- Extended Medicine duplicate-ingredient detection within reviewed `cn` detail coverage by splitting simple ingredient strings and matching shared tokens, without claiming cross-source normalization.
