# 2026-06-23

## Auth 页面视觉优化

为登录、注册、忘记密码三个 auth 页面增加品牌 Logo、欢迎副标题、字段图标和密码可见切换。

### 受影响文件

- `lib/features/auth/presentation/widgets/auth_shell.dart` — `AuthShell` 新增 `logo`/`subtitle` 槽、三段式标题区；`AuthTextField` 新增 `prefix` 槽、密码字段自治可见切换；新增 `AuthBrandLogo`、`AuthTermsNotice`
- `lib/features/auth/presentation/pages/login_page.dart` — 接入 logo/subtitle/prefix，标题改用 `authWelcomeBack`
- `lib/features/auth/presentation/pages/register_page.dart` — 接入 logo/subtitle/prefix + 服务条款提示
- `lib/features/auth/presentation/pages/forgot_password_page.dart` — 接入 logo/subtitle/prefix
- `test/auth/auth_widgets_test.dart` — 新增 3 个 widget 测试
- `test/auth/login_page_test.dart` — 修复 WeChat 按钮因页面变高被挤出视口的 `ensureVisible` 问题

### 新增 l10n key

| key | en | zh |
|---|---|---|
| `authLoginSubtitle` | Sign in to continue your health tracking | 登录以继续你的健康记录 |
| `authRegisterSubtitle` | Join Luminous and start your health journal | 加入 Luminous,开启你的健康记录 |
| `authForgotPasswordSubtitle` | We'll send a code to reset your password | 我们将发送验证码以重置你的密码 |
| `authTermsAgreement` | By creating an account, you agree to the {terms} and {privacy} | 创建即表示同意{terms}与{privacy} |
| `authTermsOfService` | Terms of Service | 服务条款 |
| `authPrivacyPolicy` | Privacy Policy | 隐私政策 |
| `authTermsComingSoonToast` | Terms and privacy policy will be available soon. | 服务条款与隐私政策即将上线。 |

`authWelcomeBack`("Welcome back"/"欢迎回来")此前已存在但未被使用，本次正式启用。

---

## 手写 copyWith 迁移至 freezed（74 类）

将全量手写数据类迁移至 freezed，消除手动 copyWith 维护成本。

### 第一批：presentation state（6 类）

| 类 | 文件 |
|----|------|
| `ShellState` | `shell_provider.dart` |
| `NotificationSettingsState` | `notification_settings_controller.dart` |
| `MedicineSearchState` | `search_provider.dart` |
| `RecordNlpState` | `record_nlp_controller.dart` |
| `RecordNlpCandidateDraft` | `record_nlp_controller.dart` |
| `AssistantState` | `assistant_controller.dart` |

### 第二批：领域实体（60 类）

| 文件 | 类数 |
|------|------|
| `report_dashboard.dart` | 7 |
| `search_entities.dart` | 5 |
| `today_dashboard.dart` | 10 |
| `mine_dashboard.dart` | 8 |
| `record_dashboard.dart` | 9 |
| `daily_record.dart` | 5 |
| `daily_record_inputs.dart` | 4 |
| `medicine_risk_check.dart` | 4 |
| `medicine_workspace.dart` | 8 |

`AppThemeSurface` 保持手写（`ThemeExtension` 框架契约要求）。

---

## 架构改进

- `lucent_envelope.dart` 内联 Map cast → `coerceToStringMap()`（第 5 处去重）
- login 页 `AnimatedSwitcher` → `flutter_animate` 的 `.animate().fadeIn().slideX()`
- 路由动画拆分：auth 页面 400/280ms（fade），CRUD 页面 220/150ms（slide+fade）

---

## ShellPage 测试补充

`test/app/shell_page_test.dart` 新增 2 个测试：
- 5 标签逐一切换无崩溃
- Mine 页面 status badge 渲染（过敏、用药、隐私）
