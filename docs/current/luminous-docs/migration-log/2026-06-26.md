# Migration Log - 2026-06-26

## 目标

统一 Luminous 的弹窗交互范式，减少底部弹出滥用，改善 Record 等页面的移动端体验。

## 改造范围

全部 10 处 `showModalBottomSheet` 调用统一改造：

| 原组件                                        | 新组件                                 | 交互形态                                                      |
| --------------------------------------------- | -------------------------------------- | ------------------------------------------------------------- |
| `RecordFastEntrySheet`                        | `RecordFastEntryDialog`                | 系统 `AlertDialog`                                            |
| `RecordNlpSheet`                              | `RecordNlpDialog`                      | 自定义居中弹窗（手机 90% 宽 / 平板最大 600dp / 最大高度 85%） |
| `showMedicineReminderDeleteSheet`             | `showMedicineReminderDeleteDialog`     | 标准 `AlertDialog`                                            |
| `showMedicineAddPrecheckSheet`                | `showMedicineAddPrecheckDialog`        | 自定义居中弹窗（手机 90% 宽 / 平板最大 480dp / 最大高度 80%） |
| `ReportRangePickerSheet`                      | `showReportRangePickerDialog`          | 系统 `AlertDialog`                                            |
| Settings `_showHelpSheet` / `_showAboutSheet` | `_showHelpDialog` / `_showAboutDialog` | 系统 `AlertDialog`                                            |
| `AssistantRecentConversationSheet`            | `AssistantConversationDrawer`          | 右侧滑出 `Drawer`（手机 80% 宽 / 平板 360dp）                 |

## 共享组件调整

- `PageScaffoldShell` 新增 `drawer`、`endDrawer`、`scaffoldKey` 参数；当提供 drawer 时用 `Scaffold` 包裹内容，否则保持原有 `Material` 行为。
- `AssistantPage` 使用 `endDrawer` + `GlobalKey<ScaffoldState>` 打开最近对话侧边栏。

## 删除的旧文件

- `lib/features/record/presentation/widgets/record_fast_entry_sheet.dart`
- `lib/features/record/presentation/widgets/record_nlp_sheet.dart`
- `lib/features/medicine/presentation/widgets/reminder/medicine_reminder_delete_sheet.dart`
- `lib/features/report/presentation/widgets/report_range_picker_sheet.dart`
- `lib/features/assistant/presentation/widgets/assistant_recent_conversation_sheet.dart`

## 行为约定

- 全部弹窗允许点击遮罩关闭 + 返回键关闭。
- 全部弹窗使用 Flutter 默认淡入缩放动画，统一维护。
- 破坏性确认（删除提醒）使用红色按钮的标准 `AlertDialog`。

## 验证

- `flutter analyze`：0 issues
- `flutter test`：全绿

## 睡眠提醒 ARB 接入 + 轻量心情记录入口

完成睡眠提醒偏好的文案本地化和心情 quick-entry，两者均只涉及 Luminous 前端，后端零改动。

### What changed

**Luminous frontend**

- `lib/l10n/app_zh.arb` / `app_en.arb`
  - 新增睡眠提醒相关 key：`settingsNotificationsSleepReminderTitle`、`settingsNotificationsSleepBedtime`、`settingsNotificationsSleepWakeTime`、`settingsNotificationsTimeUnset`、`mineReminderSleepOff`、`mineReminderSleepSummary`（带 `bedtime`/`wakeTime` 占位符）。
  - 新增心情 fast-entry key：`recordFastChoiceMoodGreat/Good/Okay/Bad/Terrible`。
  - 新增心情时间线 key：`recordTimelineMoodGreat/Good/Okay/Bad/Terrible`。
  - 运行 `flutter gen-l10n` 重新生成 `AppLocalizations`。
- `lib/features/settings/presentation/pages/notification_settings_page.dart`
  - 将睡眠提醒 section 的硬编码占位字符串替换为 `AppLocalizations`。
- `lib/features/settings/presentation/pages/settings_page.dart`
  - `_sleepReminderSummary` 改为使用 `l10n.mineReminderSleepOff` 与 `l10n.mineReminderSleepSummary(...)`。
- `lib/features/record/presentation/widgets/record_fast_entry_dialog.dart`
  - `DailyRecordKind.mood` 的 5 个 chip 标签接入 `AppLocalizations`。
- `lib/features/record/domain/entities/record_dashboard.dart`
  - `RecordCopyKey` 新增 `timelineMoodGreat/Good/Okay/Bad/Terrible`。
- `lib/features/record/presentation/widgets/record_copy.dart`
  - 映射新的心情时间线 key。
- `lib/features/record/data/repositories/lucent_record_repository.dart`
  - `_moodPayloadValue` 改为 `_moodValueKey`，返回 `RecordCopyKey?` 并写入 `RecordTimelineEntry.valueKey`，避免数据层硬编码中文。
- `lib/features/record/presentation/widgets/record_dashboard_tokens.dart`
  - 调整 `buildMobileQuickActions` 顺序：症状、饮食、用药、备注、饮水、睡眠、情绪。
- `lib/features/record/presentation/widgets/record_quick_entry_panel.dart`
  - 快速记录面板改为“分别做然后组合”：新增 `_QuickRecordGrid2x2` 负责上方 2×2 网格，新增 `_QuickRecordRow3` 负责下方 1×3 行，两者平分宽度后再用分隔线组合。
  - 上方两行两列放症状/饮食/用药/备注，下方一行三列放饮水/睡眠/情绪。
- `test/record/record_page_test.dart`
  - 更新心情时间线断言：验证 `valueKey` 与 `recordCopy(...)` 解析后的中文文案。

### Verification

- `cd Luminous && flutter gen-l10n` ✅
- `cd Luminous && flutter analyze` ✅
- `cd Luminous && flutter test test/settings/notification_settings_controller_test.dart test/settings/settings_page_test.dart test/record/record_page_test.dart test/record/record_type_mapping_test.dart test/app/shell_page_test.dart` ✅
- `cd Luminous && dart run tool/run_daily_checks.dart` ✅

## Medicine 安全提示接入后端 API

将 Medicine 页面的安全提示区从硬编码 4 条改为调用 Lucent `GET /api/v1/medicines/safety-tips`，并实现"换一批"相邻去重。

### What changed

**Lucent backend**

- 新增 `MedicineSafetyTip` Prisma 模型（`medicine_safety_tips` 表），字段包括 `contentZh`、`contentEn`、`category`、`sortOrder`、`isActive`。
- 新增 migration `20260626071858_add_medicine_safety_tips`，包含 12 条初始提示的 seed SQL。
- 新增 `MedicineSafetyTipResponseDto`。
- 在 `MedicinesController` 新增 `GET /api/v1/medicines/safety-tips`，支持 `exclude` query param 用于相邻去重。
- 在 `MedicinesService` 新增 `getRandomSafetyTips`，根据 `Accept-Language` 返回中文或英文文本，随机取 4 条，并排除上一次返回的 id。
- 在 AdminJS 注册 `MedicineSafetyTip` 资源，支持 CRUD；`category` 提供下拉枚举。
- 新增/更新 `medicines.controller.spec.ts` 与 `medicines.service.spec.ts` 测试。
- 导出更新后的 `docs/openapi.json`。

**Luminous frontend**

- Regenerated `packages/lucent_openapi`。
- 新增 `MedicineSafetyTip` entity。
- 新增 `SafetyTipsRemoteDataSource`。
- 新增 `medicineSafetyTipListProvider`（`AsyncNotifier`）管理当前提示列表与刷新行为。
- 新增 `medicine_safety_tip_style.dart`，按 `category` 映射图标与颜色。
- 重写 `_SafetyTipsSection`：移除硬编码 4 条提示，改为从 provider 加载，支持 shimmer skeleton、错误重试、"换一批"真实刷新。
- 清理 ARB：移除 `medicineSafetyTipSpacing`/`Coffee`/`Timing`/`Storage`，保留标题与刷新动作 key。
- 更新 `test/medicine/medicine_page_test.dart` 通过。

**Docs**

- 更新 `Luminous/docs/Current_State.md`：Medicine 行加入后端驱动的安全提示。
- 更新 `Luminous/docs/Next_Plan.md`：移除 deferred 项 "Medicine safety-tip '换一批'..."。

### Verification

- `cd Lucent && pnpm lint:check && pnpm build && pnpm test:ci && pnpm export:openapi`
- `cd Luminous && flutter pub get && flutter gen-l10n && dart run build_runner build && flutter analyze && flutter test`

### TODOs

- 后续可继续丰富提示库（目前 12 条），扩展更多 category。

## 注册页体验完善：跳转登录、确认密码、验证码倒计时

完善 `RegisterPage` 的创建账号流程，使其符合常规注册页交互预期。

### What changed

**Luminous frontend**

- `lib/features/auth/presentation/pages/register_page.dart`
  - 注册成功后不再自动登录，而是显示 "注册成功，请登录" 提示并跳转 `/login`。
  - 新增确认密码输入框，并与密码框同时支持右侧小眼睛切换可见/隐藏。
  - 提交前校验确认密码是否为空，并校验两次输入密码是否一致。
  - 发送验证码按钮在倒计时期间点击时，前端直接提示剩余秒数，不再向后端发请求。
- `lib/features/auth/presentation/providers/register_form_provider.dart`
  - 新增 `confirmPassword` 字段与 `validatePasswordMatch`。
  - 验证码发送成功后启动 1 秒周期 `Timer` 递减 `cooldownSeconds`，倒计时归零自动清理；`ref.onDispose` 时取消计时器避免泄漏。
- `lib/features/auth/data/datasources/auth_remote_data_source.dart`
  - `register()` 不再调用 `_client.writeSession()`，避免注册成功即写入本地会话。
- `lib/l10n/app_zh.arb` / `app_en.arb`
  - 新增 `authRegisterSuccess`、带 placeholder 的 `authCodeResendWait`。
- `test/auth/register_page_test.dart`
  - 更新输入框索引，新增确认密码。
  - 断言注册后未登录并导航到 `LoginPage`。

**Lucent backend**

- 后端 `VerificationCodeService.send()` 已保留同邮箱 60 秒冷却校验（`ResultCode.VERIFICATION_CODE_COOLDOWN`），前端拦截仅作为 UX 增强；本次未改动后端代码，仅复核测试通过。

### Verification

- `cd Luminous && dart run tool/run_daily_checks.dart` ✅
- `cd Lucent && pnpm test -- verification-code.service.spec.ts` ✅

### TODOs

- 登录页验证码模式与忘记密码页的验证码按钮目前仍只有静态冷却数字，如需同样支持倒计时，可复用本次 `RegisterFormNotifier` 的计时器模式。

## Auth 验证码倒计时统一 + 顶部图标明确使用 app icon

把登录页验证码模式、忘记密码页的静态冷却数字统一为注册页同款的 60 秒倒计时，并确保 auth 顶部品牌图标明确打包 `assets/icons/luminous_app_icon.png`。

### What changed

**Luminous frontend**

- `lib/features/auth/presentation/providers/login_form_provider.dart`
  - 新增 `cooldownSeconds` 状态与 1 秒周期 `Timer`，发送验证码成功后自动倒计时；`ref.onDispose` 取消计时器。
  - `sendCode()` 改为 `Future<bool>` 并启动计时器。
- `lib/features/auth/presentation/pages/login_page.dart`
  - 验证码模式按钮根据 `state.cooldownSeconds` 显示 “发送验证码 / N 秒后重发”。
  - 倒计时期间点击直接 toast 提示剩余秒数，不发请求。
- `lib/features/auth/presentation/providers/password_reset_provider.dart`
  - 同样加入 `Timer` 倒计时，与注册/登录页统一。
- `lib/features/auth/presentation/pages/forgot_password_page.dart`
  - 倒计时期间点击 toast 提示剩余秒数，不发请求（按钮文案已支持倒计时）。
- `pubspec.yaml`
  - 在 `assets:` 下显式增加 `- assets/icons/luminous_app_icon.png`，确保 auth 顶部 `AuthBrandLogo` 能稳定加载该图标（此前仅声明 `- assets/`）。

### Verification

- `cd Luminous && dart run tool/run_daily_checks.dart` ✅

## Auth 页面统一返回按钮

为登录、注册、忘记密码三个 Auth 页面加上左上角返回按钮，点击返回上一级路由；若当前已是路由栈底（如直接 deep link 进入），则回到首页 `/`。

### What changed

**Luminous frontend**

- 新增 `lib/features/auth/presentation/widgets/auth_back_button.dart`：
  - `AuthBackButton` 使用 `Icons.arrow_back_ios_new_rounded`。
  - 优先 `context.pop()` 返回上一级；无法 pop 时 `context.go('/')`。
- `lib/features/auth/presentation/widgets/auth_shell.dart`：导出 `AuthBackButton`。
- `lib/features/auth/presentation/pages/login_page.dart` / `register_page.dart` / `forgot_password_page.dart`：
  - 给 `AuthShell` 传入 `leading: const AuthBackButton()`。
- `test/auth/forgot_password_page_test.dart`：修复因页面变长导致提交按钮被挤出视口的测试，使用 `ensureVisible` 后点击。

### Verification

- `cd Luminous && dart run tool/run_daily_checks.dart` ✅

## Toast 体验优化与 Auth 表单行内校验

优化全局 Toast 的防重、关闭与刘海避让，并把登录/注册/忘记密码的表单空值/格式/一致性校验从 Toast 改为输入框下方红色提示。

### What changed

**Luminous frontend**

- `lib/core/feedback/app_toast.dart`
  - 新增 `_currentMessage` 实现“阀门”逻辑：同一条消息重复触发时，不重新排队，而是重置倒计时（1.8s）。
  - Toast 顶部距离由 `MediaQuery.viewPaddingOf(context).top` + `AppSpacingTokens.x4l` 计算，避开刘海/灵动岛安全区。
  - 左右边距改为 `AppSpacingTokens.md`，宽度更自适应。
  - 新增右侧关闭按钮（`Icons.close`，16px，黑色），移除 `IgnorePointer` 以支持点击关闭。
  - `_AppToastSurface` 改为 `Row` 布局，文本居中，关闭按钮固定在右侧。
- 新增 `lib/features/auth/presentation/widgets/auth_field_error.dart`
  - `AuthFieldError` 组件：按主题 `surface.error` 显示输入框下方红色提示文本，无错误时返回 `SizedBox.shrink()`。
- `lib/features/auth/presentation/widgets/auth_shell.dart`
  - 导出 `auth_field_error.dart`。
- `lib/features/auth/presentation/pages/login_page.dart`
  - 邮箱、密码、验证码字段分别包裹 `Column` 并追加 `AuthFieldError`。
  - 提交时调用 `LoginFormNotifier.validate(...)`，空值/邮箱格式错误直接以行内错误显示，不再弹 Toast。
  - 发送验证码前调用 `validateEmailOnly(...)`，邮箱为空时显示行内错误。
- `lib/features/auth/presentation/pages/register_page.dart`
  - 所有字段使用 `AuthFieldError`；提交时调用 `RegisterFormNotifier.validate(...)`，校验邮箱、验证码、密码、确认密码及两次密码一致性。
- `lib/features/auth/presentation/pages/forgot_password_page.dart`
  - 同样使用行内错误展示邮箱、验证码、密码、确认密码的校验结果。
- `lib/features/auth/presentation/providers/login_form_provider.dart`
  - 状态新增 `emailError`、`passwordError`、`codeError`。
  - 新增 `validate(...)`、`validateEmailOnly(...)`、`_isValidEmail(...)`。
  - 输入更新时同步清空对应字段错误。
- `lib/features/auth/presentation/providers/register_form_provider.dart`
  - 状态新增字段错误，新增 `validate(...)` 校验邮箱格式与密码一致性。
- `lib/features/auth/presentation/providers/password_reset_provider.dart`
  - 状态新增字段错误，新增 `validate(...)`。
- `lib/l10n/app_zh.arb` / `app_en.arb`
  - 新增 `authEmailRequiredError`、`authEmailInvalidError`、`authCodeRequiredError`、`authPasswordRequiredError`、`authConfirmPasswordRequiredError`、`authPasswordsDoNotMatchError`。

### Verification

- `cd Luminous && dart run tool/run_daily_checks.dart` ✅

## Shell 底部/侧边栏标签截断与紧凑化

对 `ShellPage` 的五个底部 tab 标签和桌面侧边栏标签增加显式截断与文字缩放限制，避免小屏设备或大字体设置下标签溢出/重叠。

### What changed

**Luminous frontend**

- `lib/features/shell/presentation/shell_page.dart`
  - 手机底部 `NavigationBar` 外层包裹 `MediaQuery.withClampedTextScaling(maxScaleFactor: 1.2)`，限制 tab 标签过度放大。
  - `NavigationBarThemeData.labelTextStyle` 增加 `overflow: TextOverflow.ellipsis`，确保超长标签显示省略号。
  - 桌面侧边栏 `_DesktopSidebar` 整体包裹 `MediaQuery.withClampedTextScaling(maxScaleFactor: 1.15)`。
  - `_DesktopSidebarItem` 与 `_DesktopSidebarActionItem` 中的标签文本改用 `Expanded` + `maxLines: 1` + `overflow: TextOverflow.ellipsis`，防止固定宽度 232px 内文字溢出。
- `test/app/shell_page_test.dart`
  - 新增测试：在 `textScaleFactor = 2.0` 下 `ShellPage` 仍能正常渲染底部导航且无未捕获异常。

### Verification

- `cd Luminous && dart run tool/run_daily_checks.dart` ✅

## 设置页新增本地睡眠提醒偏好

在 `NotificationSettingsController` 中新增本地睡眠提醒开关与就寝/起床时间偏好，并在设置页与通知设置页展示可配置项。本次只改 Dart 代码与测试，ARB key 以文本清单形式提交给产品统一写入。

### What changed

**Luminous frontend**

- `lib/features/settings/presentation/providers/notification_settings_controller.dart`
  - `NotificationSettingsState` 新增 `sleepReminderEnabled`（默认 `false`）、`sleepBedtime`（默认 23:00）、`sleepWakeTime`（默认 07:00）。
  - 新增 SharedPreferences key：`settings.notifications.sleepReminderEnabled`、`settings.notifications.sleepBedtime`、`settings.notifications.sleepWakeTime`。
  - 新增 setter `setSleepReminderEnabled(bool)`、`setSleepBedtime(TimeOfDay?)`、`setSleepWakeTime(TimeOfDay?)`。
  - `build()` 从 prefs 读取并解析 `HH:mm` 格式；缺失或解析失败时使用默认值。
  - `reset()` 清除上述三个 key 并重置状态。
  - 新增 `_formatTime` / `_parseTime` 辅助方法。
- `lib/features/settings/presentation/pages/notification_settings_page.dart`
  - 新增“睡眠提醒”section：开关行 + 就寝时间行 + 起床时间行。
  - 点击时间行弹出 `showTimePicker`，选择后持久化；开关关闭时时间行仍可显示但点击为空操作。
  - 时间未设置时显示占位文案“未设置”。
- `lib/features/settings/presentation/pages/settings_page.dart`
  - “睡眠提醒”行 trailing 改为状态摘要：未开启显示“未开启”，已开启显示 `HH:mm - HH:mm` 范围。
- `lib/features/settings/presentation/providers/notification_settings_controller.freezed.dart`
  - 通过 `dart run build_runner build -d` 重新生成。
- `test/settings/notification_settings_controller_test.dart`（新增）
  - 覆盖默认值、持久化读取、开关/时间 setter、reset 清除。
- `test/settings/settings_page_test.dart`
  - 更新默认状态断言；新增“睡眠提醒已开启时显示时间范围”的 widget test。

### Verification

- `cd Luminous && dart run build_runner build -d` ✅
- `cd Luminous && flutter analyze` ✅
- `cd Luminous && flutter test test/settings/notification_settings_controller_test.dart test/settings/settings_page_test.dart` ✅

### TODOs

- ✅ ARB key 已写入，`AppLocalizations` 替换完成（详见上方“睡眠提醒 ARB 接入 + 轻量心情记录入口”）。

## Medicine 安全后续：拆分 lactation 字段 + 年龄阈值调整

将 CN 药品说明书的 `pregnancyLactation` 拆分为 `pregnancy` / `lactation` 两个字段，并把儿科/老年年龄阈值改为包含边界。

### What changed

**Lucent backend**

- `src/modules/medicines/dto/medicine-detail.dto.ts`
  - `CnMedicineDetailDto` 新增 `pregnancy` 与 `lactation` 字段。
  - 原 `pregnancyLactation` 标记为 `deprecated`，保留以兼容旧客户端。
- `src/modules/medicines/sources/cn-medicines.service.ts`
  - 新增 `splitPregnancyLactation` 辅助函数，按关键词（孕妇/孕期/妊娠/怀孕/胎儿… vs 哺乳/哺乳期/乳母/乳汁/授乳/喂奶/母乳…）对源文本分句。
  - 一句同时命中两类关键词时，会同时写入 `pregnancy` 和 `lactation`。
  - 未命中任何关键词的文本整体回退到两个字段，避免信息丢失。
- 导出更新后的 `docs/openapi.json`。

**Luminous frontend**

- Regenerated `packages/lucent_openapi`。
- `lib/features/medicine/domain/services/medicine_risk_checker.dart`
  - 孕期/哺乳期风险分别读取 `detail.pregnancy` / `detail.lactation`。
  - 保留对旧 `pregnancyLactation` 的互斥过滤作为降级（仅在新字段缺失时生效）。
  - 儿科/老年阈值由 `< 18` / `> 65` 改为 `<= 18` / `>= 65`。
- `test/medicine/medicine_risk_checker_test.dart`
  - `_detail` helper 新增 `pregnancy` / `lactation` 参数。
  - 年龄边界测试：18 岁/65 岁预期由“不触发”改为“触发”。
  - 孕期/哺乳期互斥泄漏测试改为使用拆分后的字段构造 fixture。

**Docs**

- 更新 `Luminous/docs/Current_State.md`：Medicine 风险检查阈值与 pregnancy/lactation 字段描述同步为最新行为。
- 删除已完成计划 `Lucent/plans/2026-06-26_medicine_safety_followup.md`。

### Verification

- `cd Lucent && pnpm lint:check && pnpm build && pnpm test:ci && pnpm export:openapi` ✅
- `cd Luminous && dart run tool/regenerate_lucent_openapi.dart` ✅
- `cd Luminous && flutter analyze` ✅
- `cd Luminous && flutter test test/medicine/medicine_risk_checker_test.dart` ✅
- `cd Luminous && dart run tool/run_daily_checks.dart` ✅

## Review refactor: 抽离公共层、统一组件、引入第三方库

按 `Luminous/docs/review/report-20260626-deep.md` 完成高/中/低优先级重构与文档沉淀。

### What changed

**Lucent backend**

- 新增 `src/common/ai/ai-safety-policy.service.ts`
  - 统一 AI 内容安全策略；默认硬编码医疗建议禁用词。
  - 支持 `AI_SAFETY_FORBIDDEN_PATTERNS` 环境变量覆盖（逗号/换行分隔的正则字符串）。
- 新增 `src/common/ai/base-ai-generator.service.ts`
  - 泛型基类统一 `generate` / `generateStream` / 工具调用配置 / stream 解析。
  - `ReportsAiSummaryGeneratorService` 与 `TodayAnalysisGeneratorService` 改为继承实现。
- 新增 `src/common/ai/base-ai-summary.service.ts`
  - 统一 AI summary orchestration：enable 检查、fallback、policy 过滤、stream fallback、持久化钩子。
  - `ReportsAiSummaryService` 与 `TodayAnalysisService` 改为继承实现。
- 删除旧的 `reports-ai-summary-policy.service.ts` / `today-analysis-policy.service.ts` facade，统一使用 `AiSafetyPolicyService`。
- `src/config/ai.config.ts` 新增 `safety.forbiddenPatterns`，`src/config/env-keys.enum.ts` 与 `src/config/environment.validation.ts` 新增 `AI_SAFETY_FORBIDDEN_PATTERNS`。
- `src/modules/llm-runtime/llm-runtime.service.ts` 的 `AiRole` 排除 `safety` 字段。
- `src/common/utils/date-time.utils.ts` 使用 `date-fns` 的 `format` / `parseISO` 替代手写日期处理。

**Luminous frontend**

- 新增 `lib/features/auth/presentation/providers/auth_form_mixin.dart`
  - `AuthValidationMixin`：统一 email/code/password/confirmPassword 校验，使用 `email_validator`。
  - `CooldownTimerMixin<T>`：统一验证码倒计时 Timer 生命周期。
  - `RegisterFormNotifier` 与 `PasswordResetNotifier` 改为 with 这两个 Mixin。
- 新增 `lib/core/widgets/app_dialog.dart`
  - 统一 `Dialog` + `ConstrainedBox` + `SafeArea` + `Padding` + 滚动 的 dialog 外壳。
  - `RecordNlpDialog` 与 `MedicineAddPrecheckDialog` 已迁移使用。
- `RecordNlpDialog` 从 `ConsumerStatefulWidget` 迁移为 `HookConsumerWidget`，使用 `useTextEditingController` 替代手动 `initState`/`dispose`。
- `pubspec.yaml` 新增依赖：`email_validator`、`flutter_hooks`、`hooks_riverpod`。

**Docs**

- `Lucent/docs/public/assistant-contract.md`：更新 AI Architecture Boundary，新增 AI Safety Policy 章节。
- `Lucent/docs/README.md`：Update Map 增加 AI 抽象/安全规则变更对应文档。
- `Lucent/docs/environment.md`：增加 `AI_SAFETY_FORBIDDEN_PATTERNS` 说明。
- `Luminous/docs/Project_Guardrails.md`：新增 AppDialog、Auth Mixin、flutter_hooks、email_validator、mounted 检查等约束。
- `Luminous/docs/Current_State.md`：记录 AppDialog、Auth Mixin、email_validator、flutter_hooks 已落地。

### Verification

- `cd Lucent && pnpm lint:check && pnpm build && pnpm test today-analysis.service reports-ai-summary.service` ✅
- `cd Luminous && flutter analyze` ✅
- `cd Luminous && flutter test test/auth/` ✅
- `cd Luminous && flutter test test/record/` ✅
