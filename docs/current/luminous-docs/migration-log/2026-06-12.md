# 2026-06-12

## Report frontend contracts

- wired `Report` to the new Lucent `/api/v1/user/reports/dashboard` contract instead of the old static mock-only provider path
- added `ReportRemoteDataSource` and `LucentReportRepository`, and mapped generated OpenAPI report DTOs into a dynamic frontend report domain model
- removed `ReportCopyKey`-driven fake data modeling so score summary, findings, pattern text, metric values, and trend values now come from the backend contract
- changed Report page behavior to fetch real data on entry for signed-in users, keep skeleton loading during fetch, and show an explicit signed-out gate instead of rendering a fake report as the main path
- kept report export cards as frontend placeholders because export contracts/files are still out of scope for this phase
- updated report widget tests to cover signed-in rendering, loading skeleton behavior, and signed-out gating against the new provider flow

## Report chrome and refresh cleanup

- removed remaining report-layer hardcoded English chrome from repository/entity mapping and moved metric titles, summary mode copy, and export card copy back under frontend l10n ownership
- changed report summary/export domain modeling to enum-driven UI chrome so the data layer only carries dynamic backend content and stable action kinds
- added mobile `RefreshIndicator` as the primary manual refresh path for Report while keeping the explicit sync button as a fallback action
- updated report widget tests to cover pull-to-refresh without relying on duplicated loading copy

## Report signed-out state alignment

- changed Report signed-out behavior from a full-page auth error gate to an in-tab placeholder dashboard with an inline login notice
- aligned Report with the existing `Today / Record / Mine` protected-tab pattern: signed-out root tabs keep their chrome and placeholder sections instead of rendering an error page
- kept protected-data requests blocked while signed out; the signed-out dashboard is local placeholder data only

## OpenAPI generated-noise cleanup

- updated `tool/regenerate_lucent_openapi.dart` to pass generator global properties that disable generated Markdown docs and generated package stub tests
- added a post-generation cleanup step that removes legacy `packages/lucent_openapi/doc/` and `packages/lucent_openapi/test/` directories and strips those entries from `.openapi-generator/FILES`
- kept the generated Dart client itself committed and analyzable; only low-value doc/test noise is being removed

## Report phase-2 closeout and review triage

- folded the completed report-contract plan back into `Current_State.md`, `Next_Plan.md`, and guardrails, then removed the now-finished `plans/2026-06-12-report-contracts.md`
- reviewed the external `problum.md` audit instead of importing it blindly; kept only the findings that still reflect real unresolved work
- confirmed that guest preview repositories for signed-out root tabs are intentional current architecture, not accidental production data-source drift
- confirmed that Report already has repository-level real-contract mapping coverage, so the remaining gap is the remote-data-source/request path rather than "report has only mock tests"
- carried forward the still-real backlog only:
  - sleep scope drift across Today / Record / Settings placeholder surfaces
  - user-visible `mock data` and fake medicine-name placeholder copy
  - missing frontend controller/HTTP-layer tests for settings, export, report remote fetch, and health-context writes
  - Lucent config hardening around fallback secrets plus `testing-support` argon2 option alignment

## Today AI analysis manual path

- wired Today's AI summary card to the real Lucent `POST /api/v1/user/today-analysis/generate` contract instead of keeping it as a static placeholder-only panel
- added frontend Today AI entity/data-source/repository/provider layers and kept the card non-blocking relative to the rest of the Today page
- aligned the card with existing auth/settings boundaries:
  - signed-out users keep the card shell and see a login-required hint
  - users with `aiSummariesEnabled=false` see a settings-directed disabled state
  - loading, success, and retryable error states stay local to the card
- split Today tests by responsibility instead of continuing to grow one mixed file:
  - `today_page_test.dart` for page/layout smoke coverage
  - `today_ai_card_test.dart` for widget-state coverage
  - `today_ai_analysis_provider_test.dart` for provider-state coverage
- fixed a real Riverpod initialization hazard in `TodayAiAnalysisController`: the old `ref.listen(... fireImmediately: true)` path could read/write self state during initialization

## Today AI backend locale support

- moved Today AI fallback copy and prompt-user-copy off service-local hardcoded Chinese strings and into Lucent i18n resources
- Today AI now follows request language for `zh-CN` and `en` in:
  - fallback summary/bullets/action/confidence note
  - prompt user instructions for the model call
- expanded Today AI policy blocking to catch English dosage/stop-medication wording too, not only Chinese safety-violation text

## Test structure regrouping

- moved flat `test/` files into feature folders such as `test/auth`, `test/today`, `test/record`, `test/medicine`, `test/mine`, `test/report`, and `test/settings`
- moved `integration_test/` files into feature folders plus `integration_test/support` for shared helpers
- kept file names stable and updated relative helper imports so Flutter test discovery still works with nested directories
- confirmed `flutter test` still passes after the move; `integration_test` now needs an explicit `-d <deviceId>` because the local environment exposes multiple Flutter targets

## Full-stack Today / Report lane

- added `integration_test/app/fullstack_today_report_lane_test.dart` as a new Lucent-backed Android-emulator lane
- the new lane reuses the existing full-stack login/test-user helper and verifies:
  - sign-in against Lucent test runtime
  - Today protected dashboard loads real backend-backed sections without falling into an error state
  - Report protected dashboard loads real backend-backed sections
  - Report explicit sync action can be tapped after scrolling through the page and returns to a healthy non-error state
- fixed a real mobile-only Report layout issue found by the new lane by giving the pattern/metric card grid a slightly safer row height instead of letting a 1px overflow survive on device

## Report remote-data-source coverage

- added `test/report/report_remote_data_source_test.dart` to cover the raw `/api/v1/user/reports/dashboard` request path instead of testing only repository mapping
- verified the generated client currently sends the expected default `range=last_7_days` query parameter when fetching the dashboard
- verified `ReportRemoteDataSource` unwraps Lucent's `{ code, message, data }` envelope and returns the nested dashboard DTO shape intact

## Report weekly AI summary real path

- added Lucent `POST /api/v1/user/reports/weekly-summary/generate` with locale-aware prompt/fallback copy, analysis-model gating, AI-summary setting gating, structured-output validation, and localized fallback when model output is unsafe or unavailable
- regenerated `packages/lucent_openapi` against the new weekly summary contract and updated generated-client baseline docs to 47 paths / 138 schemas
- added `ReportAiSummaryRemoteDataSource`, `LucentReportAiSummaryRepository`, and `reportAiSummaryControllerProvider` so Report weekly AI summary no longer piggybacks on dashboard-mapped fake summary data
- removed `dashboard.summary` from the Report dashboard domain model so dashboard facts and AI summary state are no longer coupled
- wired the Report top-bar generate action to the real weekly summary endpoint and kept signed-out / disabled / loading / success / error states local to the summary section
- updated Report ARB copy to reflect real behavior:
  - `Preview report` -> `Generate summary`
  - snapshot hint now says generate or sync
  - AI summary now has explicit default / generating / error / disabled hints
  - stale `mock data` report error copy has been removed
- added `test/report/report_ai_summary_repository_test.dart` and updated report page/repository tests to cover the new provider boundary and signed-in settings gating

## Remaining backlog replanning

- identified remaining test gaps as controller/HTTP-layer tests for settings, export, health-context writes, and Report AI summary
- created `plans/2026-06-12-remaining-test-followups.md` to capture the targeted test round

## Controller / HTTP-layer test round

- added `test/settings/user_settings_controller_test.dart` (12 tests):
  - initial load success / null-data / DioException paths
  - `setAiSummariesEnabled` and `setDataSharingConsent` toggle success, error propagation, and null-response handling
  - toggle independence and sequential patch ordering
- added `test/settings/data_export_controller_test.dart` (12 tests):
  - initial load with existing request, null request (no prior export), DioException, null response
  - `requestExport` success, error propagation, null response, and first-request-from-null state
  - `refresh` re-fetch, null clear, and error propagation
  - full request → refresh lifecycle tracking (requested → processing → completed)
- added `test/health_context/health_context_remote_data_source_test.dart` (15 tests):
  - `updateProfile`: PATCH path + explicit fields, noChange sentinel omission
  - allergy CRUD: POST create full/minimal body, PATCH update with id, DELETE with id
  - condition CRUD: POST create full/minimal body, PATCH update with id, DELETE with id
  - currentMedicine CRUD: POST create full/minimal body, PATCH update with id, DELETE with id
  - error: empty response body throws DioException
- added `test/report/report_ai_summary_remote_data_source_test.dart` (3 tests):
  - POST to `/api/v1/user/reports/weekly-summary/generate` with correct path and deserialization
  - DioException propagation on network failure
  - TypeError on empty response (current real behavior)
- added `test/report/report_ai_summary_provider_test.dart` (9 tests):
  - build: signed-out idle, disabled when aiSummariesEnabled=false, idle when signed-in + enabled
  - generate: signed-out returns idle, disabled returns disabled, loading → success with state capture, backend 403 → disabled, backend failure → error, previous summary retention on retry
- verified full suite: `flutter test` (230 tests) and `flutter analyze` (0 issues) both pass
- deleted completed plan file `plans/2026-06-12-remaining-test-followups.md`

- removed the now-finished `plans/2026-06-12-remaining-test-followups.md`
- added `plans/2026-06-12-remaining-backlog.md` as the new active closeout plan for the still-real remaining work
- updated `Next_Plan.md` so it no longer claims the completed settings/export/health-context/report test gaps are still pending
- narrowed the active backlog to:
  - Report weekly AI summary empty-response hardening
  - sleep placeholder scope alignment across Today / Record / Settings
  - fake medicine-name / mock placeholder copy cleanup
  - Lucent fallback secret and argon2 hardening

## Report AI summary empty-response handling

- changed `ReportAiSummaryRemoteDataSource` to treat an empty Lucent weekly-summary response as `DioExceptionType.badResponse` with an explicit empty-response error
- updated the remote data source test to assert the explicit `DioException` contract instead of the old null-assertion crash behavior

## Report coverage gap closure

- removed the now-completed report weekly AI summary empty-response item from `Next_Plan.md`
- folded the new empty-response contract test into the report test suite so the frontend coverage gap no longer appears as active backlog

## Sleep scope alignment

- changed Today's sleep vital fallback from a fake numeric value to neutral `--`
- marked Record sleep quick actions and filters as locked placeholder surfaces, with visible deferred badges and toast fallback instead of create behavior
- changed the Settings sleep reminder row to a local-only placeholder label instead of an enabled/disabled status
- updated the active plan and current-state docs so sleep placeholder work no longer reads like an unfinished backlog item

## Placeholder copy cleanup

- replaced medicine preview mock names with neutral example labels instead of real drug names
- changed the record error boundary copy to neutral wording and removed the remaining `mock data` phrasing from user-visible text
- changed Today and Mine error descriptions to neutral boundary wording so those pages no longer tell users to retry a mock fetch
- updated the related repository tests to use neutral example medicine names so fixture data no longer leaks the old placeholder terms
