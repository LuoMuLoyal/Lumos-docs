# 2026-06-30 审核修复：废弃代码清理补完

## 审核发现

2026-06-29 迁移日志声称已移除 `pregnancyState`/`lactationState` 相关代码，但实际清理未完成。
本次审核（`flutter analyze` + `flutter test` 全量通过后再做深度 grep 审查）发现了 3 个遗漏文件。

## 修复

### 废弃枚举和字段

- `health_context_write_inputs.dart`：删除 `HealthPregnancyState` 枚举（6 个值）、`HealthLactationState` 枚举（4 个值）、`HealthProfileUpdateInput` 中 `pregnancyState` / `lactationState` 构造参数和字段。

### 废弃写入逻辑

- `health_context_remote_data_source.dart`：`healthProfileUpdatePayload()` 中删除 `_putIfChanged('pregnancyState', ...)` 和 `_putIfChanged('lactationState', ...)` 两行。

### 废弃测试数据

- `health_context_remote_data_source_test.dart`：期望 payload 中删除 `'pregnancyState': null` 和 `'lactationState': null`。

### 文档更新

- `Next_Plan.md`：日期更新至 2026-06-30，Immediate Work Order #1 删除已完成的 `AuthBackButton` 统一子项。

## 验证

- `flutter analyze`: No issues found
- `flutter test`: 910 / 910 全部通过

---

## Mock 数据 Debug-Mode 门控

### 问题

项目中 5 个 dashboard/workspace provider 在未登录时直接返回 mock 数据。虽然已标记 `[DEMO]`，但 Release 构建中仍会出现假数据。

### 方案

- `kDebugMode` 下保留 mock 数据（开发预览用）
- Release 模式下返回 domain 模型自身的 `signedOut()` 工厂，提供干净的"未登录空态"

### Domain 模型新增 `signedOut()` 工厂

| 模型 | 文件 | 空态内容 |
|------|------|---------|
| `TodayDashboard` | `today_dashboard.dart` | 空体征、零计数、空列表 |
| `RecordDashboard` | `record_dashboard.dart` | 空时间线、空日历标记、标准快速操作/过滤器 |
| `MedicineWorkspace` | `medicine_workspace.dart` | 零剂量、空用药计划、仅搜索快捷操作 |
| `ReportDashboard` | `report_dashboard.dart` | 零评分、空指标/趋势/导出 |
| `MineDashboard` | `mine_dashboard.dart` | 访客档案、零完成度 |

### Provider 门控（5 个文件）

统一模式：
```dart
if (session.isConfirmedSignedOut) {
  if (kDebugMode) return Mock;           // 开发预览
  return XxxDashboard.signedOut();       // 生产空态
}
```

新增 `import 'package:flutter/foundation.dart';` 到所有 5 个 provider。

涉及文件：
- `today_dashboard_provider.dart`
- `record_dashboard_provider.dart`
- `medicine_workspace_provider.dart`
- `report_dashboard_provider.dart`
- `mine_dashboard_provider.dart`

### 未登录提示

Today/Record/Medicine 页面暂未添加显式"请登录"banner（Report/Mine 已有）。
这是在 `kDebugMode` 门控基础上的后续 UX 优化，不影响本次修复范围。

### 验证

- `flutter analyze`: No issues found
- `flutter test`: 910 / 910 全部通过

---

## 点击反馈修复（InkWell 审核）

### 严重

1. **`sleep_structured_fields.dart:189`** — `_TimePickerField` 的 `InkWell` 缺少 `Material` 祖先，涟漪完全不显示。添加 `Material(color: Colors.transparent)` 包裹。

2. **`record_quick_entry_panel.dart:302`** — 锁定的快速操作（`action.locked == true`）仍可点击，运行时触发 `assert`。onTap 守卫增加 `|| isLocked` 检查。

### 中等

3. **`record_quick_entry_panel.dart:383`** — `RecordGuideRow` 中的 `InkWell(onTap: null)` 永不可点击，替换为直接子节点。

4. **`report_findings_section.dart:73`** — 同上，移除无意义 `InkWell(onTap: null)`。

5. **`report_patterns_section.dart:84`** — 同上。

6. **`app_text_action.dart`** — `onTap == null` 时无禁用视觉样式。添加 `Opacity(opacity: 0.5)` 包裹。

7. **`medicine_workspace_parts.dart`** — `MedicineHeaderActionChip` 的 `onTap` 可空但无禁用态。添加 `Opacity(opacity: 0.5)` 包裹。

### 轻微

9. **`record_new_entry_panel.dart:55`** — 语音按钮 `onNewEntry == null` 时无禁用样式。添加 `Opacity` 包裹。

> 注：#8（`today_priority_section.dart` 嵌套重复 InkWell）跳过修复：内层 InkWell 为药片区域提供正确的独立涟漪，父级 InkWell 为行其余部分提供涟漪，属于有意为之的布局，实际影响极小。

### 验证

- `flutter analyze`: No issues found
- `flutter test`: 910 / 910 全部通过

---

## Auth 表单验证去重

### 调查

审计报告声称 `RegisterFormNotifier` 和 `PasswordResetNotifier` 有重复验证逻辑。
实际状态：两个 Notifier **已使用** `AuthValidationMixin` + `CooldownTimerMixin`（`auth_form_mixin.dart`）。
唯一未使用 mixin 的是 `LoginFormNotifier`。

### LoginFormNotifier 重构

`login_form_provider.dart` 原先手写了：
- `_isValidEmail()` — 手写 email 正则（而非 `email_validator` 包）
- `_cooldownTimer` / `_startCooldownTimer()` — 重复 `CooldownTimerMixin` 逻辑
- `validate()` 中内联的 email/密码/验证码验证 — 重复 `AuthValidationMixin` 逻辑

重构后：
- 继承 `AuthValidationMixin, CooldownTimerMixin<LoginFormState>`
- `validate()` 委托给 `validateEmail()` / `validatePassword()` / `validateCode()`
- `sendCode()` 使用 `startCooldown(getCooldownSeconds:, setCooldownSeconds:)`
- `build()` 注册 `ref.onDispose(disposeCooldown)`
- 删除：`_isValidEmail()` 静态方法、`_cooldownTimer` 字段、`_startCooldownTimer()` 方法
- 删除的手写正则：`RegExp(r'^[^/s@]+@[^/s@]+/.[^/s@]+$')` — 改用 `email_validator` 包

### TextEditingController 管理 → flutter_hooks 迁移

审计报告建议全局改用 `flutter_hooks` 管理 controller 生命周期。经调查：
- 16 个文件、53 个 controller 使用手动 `initState`/`dispose`

本次全部完成迁移：

| 文件 | Controller | 类型 |
|------|:---:|------|
| `change_email_page.dart` | 2 | 简单 |
| `register_page.dart` | 5 | 简单 |
| `forgot_password_page.dart` | 4 | 简单 |
| `profile_edit.dart` | 3 | useState 替代 setState |
| `allergy_edit.dart` | 3 | useState 替代 setState |
| `condition_edit.dart` | 3 | useState 替代 setState |
| `current_medicine_edit.dart` | 7 | useState 替代 setState |
| `sleep_structured_fields.dart` `_NumberField` | 1 | didUpdateWidget→useEffect |
| `search_input.dart` | 1 | didUpdateWidget→useEffect |
| `record_nlp_candidate_editor.dart` | 3 | didUpdateWidget→useEffect |
| `record_create.dart` | 4 | 复杂 initState→useEffect |
| `record_edit.dart` | 4 | 复杂 initState→useEffect |
| `assistant_page.dart` | 1 | ref.listenManual→ref.listen |
| `medicine_reminder_edit_page.dart` | 1 | setState→useState |
| `login_page.dart` | 5 | OAuth回调→useEffect（手工重写） |
| `account_settings_page.dart` | 6 | 用户同步→useEffect（手工重写） |

**总计**：16 文件、53 controller 全部消除手动 `initState`/`dispose`，减少 ~300 行样板代码。

### 验证

- `flutter analyze`: No issues found
- `flutter test`: 910 / 910 全部通过

---

## 测试补齐

### Page-level error 测试（3 个新增）

审计报告（已部分过时）指出 Record/Medicine/Mine 缺少 error 状态测试。实际状况：
- Today ✅ 已有（loading + error）
- Report ✅ 已有（loading + error）
- Record ⚠️ 有 loading，缺 error
- Medicine ⚠️ 有 loading，缺 error
- Mine ⚠️ 有 loading，缺 error

补齐 3 个 page-level error 测试：
- `record_page_test.dart`：覆盖 `recordDashboardProvider` 抛出异常 → `AppStateErrorView` 渲染
- `medicine_page_test.dart`：覆盖 `medicineWorkspaceProvider` 抛出异常 → `MedicineErrorView` 渲染
- `mine_page_test.dart`：覆盖 `mineDashboardProvider` 抛出异常 → `MineErrorView` 渲染

### Dio 错误映射测试（10 个新增）

`lucent_dio_client_test.dart` 新增 10 个测试覆盖 `_mapToApiException` / `_fallbackMessage`：

- 8 种 `DioExceptionType` 的中文回落消息验证（connectionTimeout、sendTimeout、receiveTimeout、badCertificate、connectionError、cancel、badResponse、unknown）
- 信使 envelope body 消息优先提取验证（含 code 字段）
- 无响应数据时的优雅降级

新增 `_FailingAdapter` 类用于模拟 DioException 抛出。

### 验证

- `flutter analyze`: info-level warnings only（avoid_dynamic_calls 为已有代码）
- `flutter test`: 910 / 910 全部通过（+13）

---

## Lucent 后端 — 6-29 审查报告全面修复

基于 `Lucent/docs/lucent-review-2026-06-29.md` 逐项修复全部 30 个问题（B1-B30）。

### 代码级修复（B1-B8）

**B1 — Apple OAuth 重复造轮子**
- `apple-oauth.provider.ts`：注入 `JwtService` 替代直接 `jsonwebtoken` 调用；`jwt.decode` → `this.jwtService.decode`，`jwt.verify` → `this.jwtService.verifyAsync`；移除 `import jwt from 'jsonwebtoken'`
- 导入重排：Node 内置 → NestJS → 第三方 → 项目内部
- 硬编码 `APPLE_JWKS_URL` / `APPLE_ISSUER` → `OAuthConfig.apple.jwksUrl` / `issuer`
- 保留 `crypto.createPublicKey`（JWK→PEM 转换仍需要）
- `oauth.config.ts`：扩展 `apple` 类型 `{ appId, jwksUrl, issuer }`
- 2 个 wechat spec 文件适配新配置类型

**B2 — 空 catch 吞异常**
- `medicines.service.ts`：`catch { /* fall through */ }` → `catch (err) { this.logger.error(...) }`，保留 null 降级

**B3 — 有偏洗牌算法**
- 新建 `common/utils/array.utils.ts`：Fisher-Yates 均匀随机排列
- `medicines.service.ts` + `today-recommendations.service.ts`：删除私有 `shuffleArray`，导入共享工具

**B4 — 手动逐字段 Prisma 映射**
- `user-health-context-mapper.service.ts`：三处 `.map()` 改用 rest-spread 模式：`({ userId: _userId, dateFields..., ...rest }) => ({ ...rest, dateFields: formatted })`。新增 Prisma 字段自动透传

**B5 — calculateAge 时区混合**
- `date-time.utils.ts`：新增 `calculateAge()`，全 UTC getter + `Math.max(age, 0)` 防御未来日期
- mapper 删除私有方法，改为导入共享工具

**B6+B7+B8 — files 模块安全加固**
- `create-file-upload.dto.ts`：新增 `@Matches(/^[a-z]+//[-a-z0-9+.]+$/i)` + `@IsPositive @IsInt` + `@MaxLength(255) @Matches(/^[^///]+$/)`
- 新建 `common/constants/mime-types.constant.ts`：`ALLOWED_IMAGE_TYPES` 共享常量
- 新建 `i18n/en/files.json` + `zh-CN/files.json`：`content_type_not_allowed`、`file_size_exceeds_limit`
- `files.service.ts`：注入 `I18nService`，错误消息改 `this.i18n.t()`；导入共享常量
- `daily-record-image-upload.service.ts` 同步修复：复用共享常量 + i18n

**B18 — systemPrompt 硬编码中文**
- `medicines.service.ts`：systemPrompt → `this.i18n.t('medicine.recognize_prompt')`
- `i18n/en/medicine.json` + `zh-CN/medicine.json`：新增 `recognize_prompt` / `recognize_user_message`

### 工程化/架构修复（B9-B17）

**B9 — 服务文件位置统一**
- 11 个模块 `git mv {name}.service.ts → services/{name}.service.ts`
- 批量 sed 修复 ~40 处模块内部导入 + 28 处跨模块导入
- `support-resources.service.ts`：`__dirname` 路径多一层 `../`

**B10 — Guard 目录滥用**
- 3 个 `guards/ownership.service.ts` → `services/ownership.service.ts`（git mv）
- 13 处导入路径更新；删除空的 `guards/` 目录

**B11 — 路由前缀统一**
- 13 个 Controller：`@Controller('user/xxx')` → `@Controller('xxx')`
- `app.module.ts`：新增 `RouterModule.register()` 集中管理 `/user/*` 前缀

**B12 — ownership 服务重复**
- `daily-records/ownership.service.ts`：手动 `if (!record || record.userId !== userId)` → `ensureOwnedByUser(record, userId, ...)` 共享 helper

**B13 — 模块导出规范**
- `assistant.module.ts`：exports 4→1（仅保留 `HistoricalAiSummaryService`）
- `medicines.module.ts`：exports 清空
- `daily-records.module.ts`：exports 新增 `DailyRecordImageUploadRuntime`
- `AGENTS.md`：追加导出规范

**B14 — files/dto 缺 index.ts**
- 新建 `files/dto/index.ts`：`export * from './create-file-upload.dto'`

**B15 — auth/dto/responses 嵌套**
- 扁平化：`responses/common.dto.ts` → `dto/auth-response-common.dto.ts`，`responses/auth-responses.dto.ts` → `dto/auth-responses.dto.ts`
- `dto/index.ts` 新增两行；2 个 controller 导入路径更新；`responses/` 目录删除

**B16 — Controller 缺失**
- `llm-runtime.module.ts` + `user.module.ts`：新增 JSDoc 说明纯内部服务设计意图

**B17 — mapper 命名碎片化**
- 三个 mapper 已统一 `{domain}-mapper.service.ts`（命名一致，仅文档化）
- `AGENTS.md` 治理规则追加 mapper/ownership 命名规范

### 错误处理统一（B8 扩展）

**全局 i18n 错误消息**
- 8 个 service 文件注入 `I18nService`，消除 14 条硬编码英文错误消息
- 新建 10 个 i18n 文件（account、assistant、daily-records、medicine-dose-logs、medicine-reminders × en+zh-CN）
- 更新 `auth.json`（en+zh-CN）新增 `invalid_temporary_token`
- 5 个 spec 文件同步适配

### AI 架构统一（B27-B30）

**B27+B28 — 已修复（审查前）**
- `BaseAiGeneratorService` + `BaseAiSummaryService` 已存在于 `common/ai/`

**B29 — DailyRecordCandidates 统一为三层架构**
- `base-ai-generator.service.ts`：硬编码 `'analysis'` → `protected abstract modelRole: AiRole`
- `reports-ai-summary-generator.service.ts` + `today-analysis-generator.service.ts`：新增 `modelRole = 'analysis'`
- `daily-record-candidates-generator.service.ts`：重构为 `extends BaseAiGeneratorService`，`modelRole = 'language'`
- 调用方 `hasLanguageModel()` → `hasAnalysisModel()`

**B30 — Stream 解析类型安全**
- `base-ai-generator.service.ts` 的 `readSummary`：`(partial as { summary?: unknown })` → Zod `.partial().safeParse()` + bracket access
- `parsePartialResult` / `parseResult` 的 `as unknown` 加注释说明

### 测试补齐（B19-B25）

**B19 — files 模块零测试**
- 新建 `files/services/files.service.spec.ts`（8 用例：正常上传、不支持类型、空 contentType、超大文件、扩展名转换、fallback .bin、null publicUrl、大小写混合）
- 新建 `files/files.controller.spec.ts`（2 用例：service 调用 + successEnvelope）

**B20 — user-health-context 测试**
- 核实已充实：1098 行 / 22 测试用例，含 null/foreign/param 验证全覆盖

**B21 — daily-records 测试**
- 新增 null 场景测试：`mockResolvedValue(null)` → `rejects.toThrow(NotFoundException)`

**B23 — E2E 数据清理**
- 核实 `cleanupDatabase()` 已按 FK 顺序清理 20 张表，11 个 e2e 文件使用

**B24+B25 — Mock 验证 + null 场景**
- 核实 account.spec 已有 6 处 `toHaveBeenCalledWith`；全项目 28 处 `mockResolvedValue(null)` 测试

### 安全 + 文档（B22, B26）

**B26 — eval/Function 使用**
- `adminjs.setup.ts`：JSDoc 扩展说明 SWC CJS 编译兼容性原因及安全性（specifier 为硬编码字面量）

**B22 — 架构文档**
- 新建 `docs/architecture.md`（151 行，2 个 Mermaid 图）：模块依赖图、AI 三层架构图、目录模板、路由分组、错误处理规范

### 治理规则（AGENTS.md 追加）

- 模块子目录白名单（Standard / Extended / Special 三级）
- 导出规范：服务被 exports 当且仅当有外部模块直接引用
- Mapper 命名：`{domain}-mapper.service.ts`；Ownership 命名：`ownership.service.ts`

### 验证

| 检查 | 修复前 | 修复后 |
|------|--------|--------|
| `pnpm typecheck` | — | ✅ |
| `pnpm lint:check` | — | ✅ --max-warnings=0 |
| `pnpm test:ci` suites | 67 | 69 |
| `pnpm test:ci` tests | 428 | 439 |
