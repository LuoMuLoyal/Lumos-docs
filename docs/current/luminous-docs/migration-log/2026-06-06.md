# Migration Log - 2026-06-06

Part of `../MigrationLog.md`.


### Documentation Cleanup

- Added current entry docs: `docs/Current_State.md`, `docs/Next_Plan.md`, and `docs/Project_Guardrails.md`.
- Moved inactive long-form design notes, reset notes, old plans, and historical error audits to the workspace root archive outside git.
- Updated Luminous and Lucent doc indexes so current work starts from state, change log, next plan, and concise guardrails.

### Migration Log Date Split

- Split the long `docs/MigrationLog.md` into date files under `docs/migration-log/`.
- Kept `docs/MigrationLog.md` as a short index and update-rule document.
- Updated docs and agent instructions so future entries go into today's date file instead of the index.

### README Repository Badges

- Added a backend repository badge from `Luminous/README.md` to `LuoMuLoyal/Lucent`.
- Added a frontend repository badge from `Lucent/README.md` to `LuoMuLoyal/Luminous`.

### State Sync And TODO Anchors

- Rechecked current docs against OpenAPI export: Lucent remains at 35 paths / 89 schemas, including the Tencent COS daily-record image presign endpoint.
- Added TODO anchors for the known gaps: Luminous record image attachments, local notification scheduling, More environment snapshot wiring, and Lucent environment snapshot module implementation.
- Updated `Lucent/README.md` active docs to include generated OpenAPI plus reminder/environment public contracts.

### Short-Term Target Alignment

- Updated `docs/Next_Plan.md` with three short-term targets: daily-record images, environment snapshot, and local reminder scheduling.
- Prioritized daily-record images first because the Lucent COS presign contract already exists.
- Moved local reminder scheduling behind environment snapshot because scheduling still needs an explicit reminder source, while environment snapshot can ship without external credentials.

### Daily Record Images And Environment Snapshot

- Implemented Luminous daily-record single-image upload: image picker, COS presigned PUT upload, attachment metadata create/update, timeline/detail image display, and signed-out dashboard guard.
- Implemented Lucent static Environment Snapshot API at `GET /api/v1/environment/snapshot` and regenerated OpenAPI / `packages/lucent_openapi`; current export reports 36 paths / 100 schemas.
- Updated `docs/Current_State.md`, `docs/OpenApi_Client.md`, and `docs/Next_Plan.md` so the next short-term target is wiring Luminous More to the environment endpoint.

### Record Date Navigation Stabilization

- Deferred Luminous More environment wiring so Record remains the short-term focus.
- Added selected-date state for Record and made the header Today / previous / next / date-picker actions reload the Lucent-backed timeline instead of only showing toast feedback.
- Fixed Lucent daily-record timeline mapping to display each record's `occurredAt` time rather than the current clock time.
- Added Record regression coverage for selected-date reload and occurredAt-based timeline time.

### Record Detail Page

- Added `/record/:id` as a Lucent-backed daily-record detail page with standard header, loading/error states, complete saved-field display, image attachment display, and edit/delete actions.
- Changed real timeline entries to open the detail page first; editing remains available from the detail page.
- Added localized detail labels and regenerated Flutter l10n output.
- Added Record regression coverage for detail rendering, timeline-to-detail navigation, and detail-page deletion.

### Lucent COS Upload Contract Sync

- Re-generated the Lucent OpenAPI client after Lucent added Tencent COS daily-record image upload signing.
- Generated `DailyRecordsApi.dailyRecordsControllerCreateImageUploadV1` plus the upload request/response DTOs; no app UI flow consumes it yet.

### Daily Record Detail And Attachment Contract

- Re-generated the Lucent OpenAPI client after Lucent added daily-record image attachment metadata and `GET /api/v1/me/daily-records/:id`.
- Record edit now loads by record id through the detail endpoint instead of falling back to a date-scoped list lookup.

### Review Fix: Daily Record Edit And Search Empty State

- Made `.env` loading optional so startup no longer depends on a checked-in `.env` asset.
- Fixed daily record edit to load records by id and allow clearing nullable text fields by sending explicit `null` values.
- Removed the unavailable scan/photo action from medicine search no-result tools and updated l10n/tests.

### Thirty-Day DeepSeek Execution Plan

- Added `docs/superpowers/plans/2026-06-06-thirty-day-lumos-deepseek-plan.md` as the next one-month execution plan for DeepSeek-style task-by-task delivery.
- The plan keeps the existing superpowers evidence bundle, branch/status baseline, Codex review protocol, and error-prevention gates while focusing the next month on identity/theme polish, auth/account closeout, daily-record quality, medicine adherence, notification boundaries, More integration planning, and full regression.


### Task 2: Visual Identity And Startup Polish Verification

- Verified Android adaptive icon: white background (#FFFFFF), 24% inset foreground across all density buckets.
- Verified Android splash screen: white background with centered splash_wordmark_icon vector in launch_screen.xml; Android 12+ system splash also white with same icon.
- Verified iOS LaunchScreen: white background with centered LaunchImage; all AppIcon sizes generated.
- Verified Web: favicon, PWA icons (192/512, maskable variants), manifest background_color: #FFFFFF, branded #app-splash gradient overlay.
- Verified Windows: app_icon.ico present; macOS: all AppIcon sizes generated.
- No icon or splash resources changed; current state confirmed as intentional from 2026-06-05 launch-screen polish.


### Task 3: Theme System Regression And Token Usage Audit

- Audited page-level spacing, radius, shadow, typography, and layout token usage across all features (today, record, medicine, mine, more, search, settings, shell, auth).
- Fixed 7 hardcoded EdgeInsets values that bypassed AppSpacingTokens: 4 mine edit pages (16→md), record create page (32→xl, 16→md), search parts (4→xxs ×2).
- Confirmed shadow usage follows project rules: AppShadowTokens.level1 for page surfaces, level4 for auth modal.
- Confirmed theme tests (palette persistence, settings flow navigation) are comprehensive — no gaps found.
- No token numeric values changed; all replacements are semantically identical.

### Task 4: Auth/Account TODO Triage And Safe Closeout

- Searched Lucent src/modules/auth + src/modules/account and Luminous lib/features/auth + lib/features/mine for TODO/FIXME markers.
- Found 8 TODOs, all in Lucent auth module (none in account module or Luminous frontend).
- Classification:
  - **Implemented (2):** OAuth config boot validation — WechatMobileOAuthProvider and WechatWebOAuthProvider now implement OnModuleInit and log a warning at startup when OAuth is not fully configured, removing the need to wait until first request.
  - **Remain TODO (6):** 2FA (product decision), session management (feature scope), OAuth-only set-password (security review), OAuth-only delete verification (depends on set-password), security notifications (needs notification infra), Apple/Google providers (platform credentials).
- All remaining TODOs rewritten with owner tag + blocked reason.
- No OAuth credentials, callback domains, or production behavior changed.

### Task 10: Notification/Reminder Contract Design

- Created Lucent/docs/public/reminder-contract.md defining the notification/reminder boundary:
  - Device-local layer: system permission, preference toggles, local-only notifications
  - Backend-owned layer: reminder schedules, notification preferences, delivery log
- Defined planned API surface: UserNotificationPreference, UserMedicineReminder, UserReminderDelivery
- Explicitly excluded: FCM/APNs push delivery, real-time/WebSocket, third-party services
- Updated ROADMAP.md and UI_Implementation_Plan.md to reference the contract

### Task 16: Test Suite Speed And Determinism Pass

- Frontend: 95 widget + unit tests pass in ~9 seconds; no flaky tests detected across two consecutive runs.
- Backend: 162 unit tests pass in ~3.7 seconds (17 suites); no flaky tests detected.
- No tests use `skip:` or platform-conditional `defaultTargetPlatform` guards.
- `pump(Duration(seconds: N))` calls (24 occurrences) are all for toast animation settling or async provider resolution — no arbitrary waits found.
- Desktop layout tests correctly use `tester.view.physicalSize` overrides, not platform checks.

### Task 18: Full Regression And Issue Ledger

- **Lucent:** lint OK, build (148 files, 0 TS issues), Prisma validate OK, unit tests (17 suites, 162 tests), OpenAPI export (34 paths / 83 schemas).
- **Luminous:** analyze (0 issues), widget tests (96 tests), APK build (debug, fluwx KGP warning only).
- No regressions detected. No new issues to track.


### Task 20: Final Handoff And Next-Month Backlog

**30-Day Plan Completed** — 20 tasks executed from 2026-06-06 through 2026-06-06 (single session).

**Lucent (dev):** 7 commits
- Auth TODO triage + OAuth boot validation
- Reminder contract document
- Environment snapshot contract document
- CHANGELOG.md created, docs aligned
- 162 unit tests, 0 TS compilation issues, 34 OpenAPI paths / 83 schemas

**Luminous (refactor):** 20 commits
- Visual identity audit (Task 2)
- Theme token normalization (Task 3)
- Auth/account TODO classification (Task 4)
- Account security UX audit (Task 5)
- Record edit/delete page (Task 6)
- Today factual data boundary + Lumi preview badge (Task 7)
- Medicine dose signed-out guard (Task 8)
- Provider invalidation matrix fix: 7 missing invalidations (Task 9)
- Reminder + environment contracts (Tasks 10, 13)
- Notification permission states + planned badge (Tasks 11, 12)
- Search sourceRef display + scan icon removed (Task 14)
- Settings/account navigation consistency (Task 15)
- Test suite audit: 96 tests, 0 flaky (Task 16)
- Documentation alignment (Task 17)
- Full regression (Task 18)
- Reviewer buffer (Task 19)
- flutter_dotenv integration

**Test totals:** Lucent 162 unit + Luminous 96 widget = 258 tests, all passing.

**Unsupported features (explicit):**
- Live reminders / push notifications (no FCM/APNs)
- OCR / barcode scanning
- Smart device integration
- Family profiles
- Real-time weather/environment data (static only)
- E2E tests (Docker unavailable in sandbox)

**Next-month backlog:** `docs/superpowers/plans/next-month-backlog.md`

### Task 13: More Real-Contract Design For Environment

- Created `Lucent/docs/public/environment-contract.md` defining the first real More integration:
  - **Environment snapshot API:** pollen, UV, air quality, temperature, humidity via `GET /api/v1/environment/snapshot`
  - **Static reference data first:** seasonal averages per region, no external API keys needed
  - **Data freshness:** `updatedAt` + `dataSource` field to distinguish static vs live data
  - **Optional advice endpoint** for contextual health suggestions (future)
- Explicitly excluded: real-time weather APIs, user location tracking, health diagnosis, push alerts
- Updated `ROADMAP.md` and `UI_Implementation_Plan.md` to reference the contract

### Task 5: Account Security UX Hardening

- Audited account settings page across four dimensions: password set/unset status, linked identity management, email verification state, and destructive action protections.
- Confirmed OAuth-only users are blocked from impossible flows: password change hidden (authPasswordUnsetManagementHint), account deletion hidden (authDeleteAccountPasswordRequiredHint), last identity unlink disabled (authIdentityUnlinkDisabledAction).
- Confirmed backend protections: unlinkIdentity rejects last sign-in method for OAuth-only users (403 FORBIDDEN), changePassword and deleteAccount reject passwordless users (401 WRONG_PASSWORD).
- Test coverage verified: frontend OAuth-only protection test + backend unit tests for password change/delete rejection + e2e for identity unlink protection.
- No backend behavior changes needed; current state confirmed as properly hardened from 2026-06-05 account management work.

### Flutter E2E Smoke Test

- Added `integration_test/app_smoke_test.dart` using Flutter's SDK `integration_test` package.
- The smoke flow launches the real `LuminousApp` with offline/mock repositories, covers the five-tab shell, opens Record create from the add action, verifies the signed-out create guard, then returns to Record and checks Medicine, Mine, and More anchors.
- Added the local device/emulator command `flutter test integration_test/app_smoke_test.dart` to README and verification docs.

### System Back Button Alignment

- Replaced custom child-page back affordances with Flutter `BackButton` in the shared settings header, account/change-email auth pages, and medicine search top bar.
- Updated settings and integration tests to assert/tap `BackButton` instead of the previous custom arrow icon.

### Settings E2E Coverage

- Extended `integration_test/app_smoke_test.dart` with a settings theme flow that enters Settings from Mine, opens Theme settings, selects dark mode, uses the system `BackButton` to return, and verifies the persisted `theme.mode`.
- Added a stable `mine-settings-action` key to the Mine settings header action so E2E does not depend on icon-only tooltip text.
- Added Settings language and notification preference E2E flows through the real app shell, including locale persistence and medication-reminder switch persistence with a fake notification permission service.
- Split Flutter E2E coverage into module files under `integration_test/` with shared helpers in `integration_test/e2e_test_helpers.dart`.
- Added a Settings More E2E flow that changes theme, palette, locale, and notification preferences, then verifies "reset defaults" restores the persisted defaults.

### Record E2E Coverage

- Extended `integration_test/app_smoke_test.dart` with a signed-in, fake-repository Record flow.
- The flow opens a real timeline record, verifies the detail page fields, enters the edit page through the detail header action, then returns with system `BackButton` navigation.
- The signed-in E2E setup now overrides Mine data with `MockMineRepository` so inactive `IndexedStack` tabs do not attempt real backend loading during Record tests.
- Added a signed-in Record create E2E flow that opens the add page from the real Record tab, saves a water record through the fake daily-record repository, and returns to the timeline.
- Added a Record date-navigation E2E flow that taps the real previous-day header action and verifies the timeline reloads for the selected date.
- Added a Record detail delete E2E flow that opens a timeline entry, confirms deletion, and verifies return to the timeline.

### Medicine E2E Coverage

- Extended `integration_test/app_smoke_test.dart` with a Medicine search route flow using offline mock search data.
- Added a signed-in Medicine search add-to-current E2E flow that enters Search from the real Medicine tab and writes the selected result into the health-context repository.
- Added a signed-out Medicine dose-action E2E flow that shows the plan item action and verifies it routes to the login page instead of calling protected dose-log APIs.
- Added a signed-in Medicine dose-action E2E flow that saves an `taken` dose log through a fake dose-log data source and keeps the user on the Medicine plan.

### Auth E2E Coverage

- Extended `integration_test/app_smoke_test.dart` with an Auth password-login flow entered from the real app navigation path.
- Added an offline Auth remote fake plus a fixed health-context snapshot override so login E2E verifies global session state without hidden backend calls.
- Reset the shared app router to `/` before each E2E pump so tests do not inherit the previous case's route location.
- Added an Account settings E2E flow from the real Mine account entry that saves nickname/avatar through the account provider and verifies the global auth session updates before returning with the system `BackButton`.
- Added Auth register and forgot-password E2E flows from the real login page entry, covering registration session application and reset-password remote submission.
- Added an Account change-email E2E flow from Mine through Account settings into the change-email page, verifying remote submission and global session email update.
- Added an Account change-password E2E flow from Mine through Account settings, verifying remote submission, local session clear, and login redirect.
- Added an Account delete E2E flow from Mine through Account settings, verifying remote submission, local session clear, and login redirect.
- Added an Account unlink-identity E2E flow from Mine through Account settings, verifying confirmation, remote submission, and global session update.

### Mine E2E Coverage

- Extended `integration_test/app_smoke_test.dart` with a signed-in Mine profile edit flow.
- The flow enters Mine from the real shell, opens the basic-profile edit page, saves birth date / height / blood type through a fake health-context repository, and verifies it returns to the Mine profile grid.
- Added Mine health-context sub-entry E2E flows for creating allergy, condition, and current-medicine records through the real Mine profile grid routes.

### E2E Scenario File Split

- Split oversized Auth, Record, and Settings E2E files into smaller scenario files: auth entry, account security, record navigation, record mutation, settings preferences, and settings more.
- Kept shared offline app bootstrapping and fake repositories in `integration_test/e2e_test_helpers.dart` so scenario files stay focused and future parallel work is less likely to edit the same test file.
- Updated README E2E examples to point at the new scenario-level test file names.

### Record Edit E2E Coverage

- Added a signed-in Record edit-save E2E flow that opens a timeline item, enters detail, edits value/unit/title/note, saves through the fake daily-record repository, and verifies return to the detail page.
- Extended the Record E2E fake daily-record repository to record update id/input assertions.

### Medicine Skipped Dose E2E Coverage

- Added a signed-in Medicine skipped-dose E2E flow that taps the real plan action and verifies the manual dose-log write uses `skipped` for today's date.

### Auth Code-Send E2E Coverage

- Added Auth E2E flows for sending codes from register, forgot-password, and change-email pages.
- Extended the shared E2E helper exports so register/change-email tests can assert the exact `AuthVerificationScene`; forgot-password uses the dedicated `forgotPassword` endpoint and is asserted separately.

### Medicine Search Signed-Out E2E Coverage

- Added a signed-out Medicine search add-to-current E2E flow that verifies tapping "加入药箱" routes to login and does not write health-context current medicines.

### Settings Notification Permission E2E Coverage

- Added a Settings notification-permission E2E flow that opens the notification page in a denied state and verifies tapping the permission row calls the injected permission service.
- Extended the E2E notification permission fake with a request counter for permission-request assertions.

### Mine Signed-Out Login E2E Coverage

- Added a Mine signed-out notice E2E flow that verifies tapping "去登录" routes to the login page.

### Record Date Action E2E Coverage

- Added Record date-action E2E coverage for next-day reload and returning to today's timeline.

### Settings Auth Footer E2E Coverage

- Added Settings footer E2E flows for signed-out login navigation and signed-in logout navigation.
- Extended the fake auth remote to record logout calls for E2E assertions.

